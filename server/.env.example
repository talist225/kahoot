# MongoDB connection string (Atlas or local)
MONGODB_URI=mongodb://localhost:27017/kahoot

# HTTP + Socket.IO port
PORT=3001

# Password for the admin panel (/admin). CHANGE THIS!
ADMIN_PASSWORD=admin123

# Optional: secret used to sign admin session tokens (defaults to a hash of the password)
# ADMIN_SECRET=some-long-random-string
